## Why does this repository contain an outer /images folder? 🤔

The purpose of /images folder is to provide a preview image of this project (display1.png) which will be fetch by a different repository (My current website) via REST API.